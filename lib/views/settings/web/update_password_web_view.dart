import 'package:flutter/material.dart';

class UpdatePasswordWebView extends StatelessWidget {
  const UpdatePasswordWebView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Update Password"),
    );
  }
}
